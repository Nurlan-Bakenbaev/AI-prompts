import React from 'react'
import {SessionProvider} from 'next-auth/react'

const Provider = () => {
  const Provider = ({children,se})
  return (
    <div>Provider</div>
  )
}

export default Provider